import { AttestationDirective } from './attestation.directive';

describe('AttestationDirective', () => {
  it('should create an instance', () => {
    const directive = new AttestationDirective();
    expect(directive).toBeTruthy();
  });
});
