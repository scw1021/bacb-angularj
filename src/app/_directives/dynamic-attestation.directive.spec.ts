import { DynamicAttestationDirective } from './dynamic-attestation.directive';

describe('DynamicAttestationDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicAttestationDirective();
    expect(directive).toBeTruthy();
  });
});
