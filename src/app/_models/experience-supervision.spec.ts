import { ExperienceSupervision } from './experience-supervision';

describe('Supervision', () => {
  it('should create an instance', () => {
    expect(new ExperienceSupervision()).toBeTruthy();
  });
});
