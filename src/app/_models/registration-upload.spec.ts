import { RegistrationUpload } from './registration-upload';

describe('RegistrationUpload', () => {
  it('should create an instance', () => {
    expect(new RegistrationUpload()).toBeTruthy();
  });
});
