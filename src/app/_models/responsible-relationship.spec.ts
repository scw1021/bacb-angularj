import { ResponsibleRelationship } from './responsible-relationship';

describe('ResponsibleRelationship', () => {
  it('should create an instance', () => {
    expect(new ResponsibleRelationship()).toBeTruthy();
  });
});
